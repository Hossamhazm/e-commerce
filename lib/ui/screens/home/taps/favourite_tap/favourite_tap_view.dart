import 'package:flutter/material.dart';

class FavouriteTap extends StatelessWidget {
  const FavouriteTap({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("FavouriteTap"),);
  }
}
